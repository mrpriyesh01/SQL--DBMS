Q=Write a solution to calculate the number of unique subjects each teacher teaches in the university.
Return the result table in any order.

code=
SELECT teacher_id, COUNT(DISTINCT subject_id) as cnt FROM teacher 
GROUP BY teacher_id;
